{
  "name": "everest-admin-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd ../../user && npm install && npm run build && cd ../admin/frontend && npm install && npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@libsql/client": "^0.17.4",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.0",
    "multer": "^2.2.0",
    "sql.js": "^1.11.0",
    "uuid": "^9.0.0"
  }
}
